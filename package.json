{
  "name": "umo-pos-backend",
  "version": "1.0.0",
  "description": "Backend API para UMO POS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0",
    "uuid": "^9.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
```

4. Click **"Commit new file"**

---

### Archivo 2: `Procfile`

1. Click **"Add file"** → **"Create new file"**
2. Nombre: `Procfile` (sin extensión, P mayúscula)
3. Contenido:
```
web: node server.js
```

4. Click **"Commit new file"**

---

### Archivo 3: `.gitignore`

1. Click **"Add file"** → **"Create new file"**
2. Nombre: `.gitignore`
3. Contenido:
```
node_modules/
.env
.DS_Store
